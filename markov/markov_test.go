//-----------------------------------------------------------------------------

//-----------------------------------------------------------------------------

package markov

import (
	"log"
	"testing"
)

//-----------------------------------------------------------------------------

func Test_MM1(t *testing.T) {
	a := []float64{
		0.3, 0.7,
		0.4, 0.6,
	}
	pi := []float64{
		0.5, 0.5,
	}
	mm, err := NewMM(2, a, pi)
	if err != nil {
		t.Error(err)
		return
	}
	s := mm.Init()
	for i := 1; i < 20; i++ {
		log.Printf("%d", s)
		s = mm.Next()
	}
}

//-----------------------------------------------------------------------------

func Test_MM2(t *testing.T) {
	mm, err := NewMM(7, nil, nil)
	if err != nil {
		t.Error(err)
		return
	}
	s := mm.Init()
	for i := 1; i < 20; i++ {
		log.Printf("%d", s)
		s = mm.Next()
	}
}

//-----------------------------------------------------------------------------
